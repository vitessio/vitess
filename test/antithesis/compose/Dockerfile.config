FROM scratch

# Note: For this Antithesis config image, docker-compose.yaml is the canonical
# compose file that is baked into the image. The repository also contains
# docker-compose.yml for other environments/legacy tooling; it may diverge,
# so keep intent in sync when making changes.
COPY docker-compose.yaml /docker-compose.yaml

# This compose file contains Antithesis-specific overrides/extensions.
COPY docker-compose.antithesis.yml /docker-compose.antithesis.yml
COPY .env /.env

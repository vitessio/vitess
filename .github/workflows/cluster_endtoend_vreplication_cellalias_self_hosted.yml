# DO NOT MODIFY: THIS FILE IS GENERATED USING "make generate_ci_workflows"

name: Cluster Self Hosted (vreplication_cellalias)
on: [push, pull_request]

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Build Docker Image
        run: docker build -f ./.github/dockerFiles/cluster_test_vreplication_cellalias/Dockerfile -t cluster_test_(vreplication_cellalias)  .

      - name: Run test
        timeout-minutes: 30
        run: docker run --rm cluster_test_(vreplication_cellalias) /bin/bash -c 'source build.env && go run test.go -docker=false -print-log -follow -shard vreplication_cellalias'

      - name: Cleanup Docker Image
        run: |
          docker images rm cluster_test_(vreplication_cellalias)
          docker volume prune -f
name: Code Freeze
on:
  pull_request:

permissions: read-all

jobs:
  build:
    name: Code Freeze
    runs-on: ubuntu-24.04
    steps:
    - name: Fail if Code Freeze is enabled
      run: |
        exit 1

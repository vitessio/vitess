[
  {
    "comment": "dual query that has to be sent down to a tablet",
    "query": "select @@max_allowed_packet",
    "v3-plan": {
      "QueryType": "SELECT",
      "Original": "select @@max_allowed_packet",
      "Instructions": {
        "OperatorType": "Route",
        "Variant": "Reference",
        "Keyspace": {
          "Name": "main",
          "Sharded": false
        },
        "FieldQuery": "select @@max_allowed_packet from dual where 1 != 1",
        "Query": "select @@max_allowed_packet from dual",
        "Table": "dual"
      }
    },
    "gen4-plan": {
      "QueryType": "SELECT",
      "Original": "select @@max_allowed_packet",
      "Instructions": {
        "OperatorType": "Route",
        "Variant": "Reference",
        "Keyspace": {
          "Name": "main",
          "Sharded": false
        },
        "FieldQuery": "select @@max_allowed_packet from dual where 1 != 1",
        "Query": "select @@max_allowed_packet from dual",
        "Table": "dual"
      },
      "TablesUsed": [
        "main.dual"
      ]
    }
  }
]
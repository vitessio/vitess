[
  {
    "comment": "Add your test case here for debugging and run go test -run=One.",
    "query": "select user.id1+user.id2 as mas from user join user_extra where user.id2 = user_extra.id order by mas",
    "plan": {}
  }
]
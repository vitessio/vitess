# Add your test case here for debugging and run go test -run=One.
"select name from properties join supervisions   using (propertyId) join managers   using (managerId) where propertyId = 1 AND managerId = 1;"
{
}
{
}

create table if not exists a(
                   one int,
                   two int,
                   three int,
                   four int,
                   five int,
                   primary key(one)
) Engine=InnoDB;